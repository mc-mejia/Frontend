import { CustomerBeneficiary } from './customer-beneficiary';

describe('CustomerBeneficiary', () => {
  it('should create an instance', () => {
    expect(new CustomerBeneficiary()).toBeTruthy();
  });
});
